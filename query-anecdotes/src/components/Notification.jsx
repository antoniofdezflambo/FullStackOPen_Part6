const Notification = () => {
  const style = {
    border: 'solid',
    padding: 10,
    borderWidth: 1,
    marginBottom: 5
  }
  
  const condition = true
  if (condition) return null

  return (
    <div style={style}>
      
    </div>
  )
}

export default Notification
